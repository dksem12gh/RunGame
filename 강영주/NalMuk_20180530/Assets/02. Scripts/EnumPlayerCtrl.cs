using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnumPlayerCtrl : MonoBehaviour {
    /*
    public bool _jumping;
    bool isGrounded;

    enum PLAYER
    {
        move, jump, idle
    };
    PLAYER player;

	// Use this for initialization
	void Start () {
        player = PLAYER.idle;
	}
	
	// Update is called once per frame
	void Update () {


        if (Input)
        {
            player = PLAYER.jump;

        }
        if (Input)
        {
            player = PLAYER.move;
        }

        Move(player);
        Jump(player, isGrounded);
	}

    void Move(PLAYER py)
    {
        if(py == PLAYER.move)
        {

        }
        else
        {
            return;
        }
    }

    void Jump(PLAYER py,bool h)
    {
        if (py == PLAYER.jump&!h) 
        {

        }
        else
        {
            return;
        }
    }

    void OnTriggerEnter(Collider other)
    {
        isGrounded = true;
    }

    void OnTriggerExit(Collider other)
    {
        isGrounded = false;
    }*/
}
